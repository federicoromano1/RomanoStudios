Esempio di Comando (Nmap) / Command Example
ITA: Ecco come un hacker controlla quali "porte" sono aperte su un server per trovare un'entrata.
ENG: Here is how a hacker checks which "ports" are open on a server to find an entrance.

Codice:

Bash
nmap -sV -T4 192.168.1.1
